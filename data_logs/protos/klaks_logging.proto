syntax = "proto3";

import "generic_log.proto";

message Klaks_LogEntry {
    string timestamp = 1;
    string level = 2;
    string message = 3;
    map<string, string> metadata = 4; // Optional additional data not registered
    Generic_log generic_log = 5;
}
